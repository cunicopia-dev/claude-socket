{
  "name": "claude-socket-plugin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MCP plugin that bridges Claude Code to WebSocket clients",
  "scripts": {
    "start": "bun install --no-summary && bun run server.ts",
    "test": "bun test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.4"
  }
}
